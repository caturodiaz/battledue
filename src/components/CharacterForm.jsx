import { useState } from 'react'
import { characterFields, emptyCharacter } from '../data/characterFields'

function CharacterForm({ character, onSave, onCancel }) { const [values, setValues] = useState({ ...emptyCharacter, ...character, abilities: character?.abilities || [] }); const [error, setError] = useState(''); function handleSubmit(event) { event.preventDefault(); if (!values.name.trim()) { setError('El nombre es obligatorio.'); return } onSave({ ...values, name:values.name.trim(), abilities:values.abilities.map((item) => item.trim()).filter(Boolean) }) } return <section className="form-panel"><div className="form-header"><h2>{character ? 'Editar personaje' : 'Nuevo personaje'}</h2><button className="icon-button" onClick={onCancel} type="button" aria-label="Cerrar formulario">×</button></div><form className="character-form" onSubmit={handleSubmit}>{characterFields.map(([name,label,type]) => <label className="field" key={name}>{label}<input name={name} type={type} value={values[name] || ''} onChange={(event) => setValues({ ...values, [name]:event.target.value })} /></label>)}<label className="field full-width">Habilidades <input value={values.abilities.join(', ')} onChange={(event) => setValues({ ...values, abilities:event.target.value.split(',') })} placeholder="Separadas por comas" /></label>{error && <p className="error full-width">{error}</p>}<div className="form-actions"><button className="button secondary" onClick={onCancel} type="button">Cancelar</button><button className="button" type="submit">Guardar personaje</button></div></form></section> }
export default CharacterForm
