function CharacterCard({ character, onEdit, onDelete }) { const subtitle = [character.species,character.alignment,character.power].filter(Boolean).join(' · ') || 'Sin detalles adicionales'; return <article className="character-card">{character.image ? <img className="portrait" src={character.image} alt={character.name} /> : <div className="portrait-placeholder" aria-hidden="true">{character.name.charAt(0).toUpperCase()}</div>}<div className="character-details"><h2>{character.name}</h2><p>{subtitle}</p></div><div className="character-actions"><button className="button secondary small" onClick={() => onEdit(character)} type="button">Editar</button><button className="button danger small" onClick={() => onDelete(character.id)} type="button">Eliminar</button></div></article> }
export default CharacterCard
